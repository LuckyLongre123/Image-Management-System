<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>All Uploaded Images</title>
  </head>
  <body class="bg-gray-100 min-h-screen flex items-center justify-center px-4">
    <div class="bg-white p-6 sm:p-8 rounded-lg shadow-lg w-full max-w-6xl">
      <h1 class="text-2xl sm:text-3xl font-bold text-center mb-6 text-gray-800">
        All Uploaded Images
      </h1>

      <div class="mb-6 text-center">
        <a
          href="/"
          class="text-blue-600 hover:text-blue-800 underline font-medium transition duration-200"
        >
          Go to Home
        </a>
      </div>

      <% if (users.length === 0) { %>
      <p class="text-center text-gray-500">No images found.</p>
      <% } else { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% users.forEach(user => { %>
        <div
          class="bg-gray-50 rounded-lg shadow-md overflow-hidden transition transform hover:scale-105 duration-300"
        >
          <img
            src="<%= user.imageUrl %>"
            alt="<%= user.name %>"
            class="w-full h-48 object-cover"
          />
          <div class="p-4">
            <h2
              class="text-lg font-semibold text-gray-700 text-center truncate"
            >
              <%= user.name %>
            </h2>
          </div>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>
  </body>
</html>
